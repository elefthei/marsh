[package]
name = "marsh-shell"
description = "A shell whose every submitted line is one capability-gated transaction"
version = "0.1.0"
authors.workspace = true
categories.workspace = true
edition.workspace = true
keywords.workspace = true
license.workspace = true
readme.workspace = true
repository.workspace = true
rust-version.workspace = true
publish = false

[lib]
bench = false

[[bin]]
name = "marsh"
path = "src/main.rs"
bench = false

[lints]
workspace = true

[dependencies]
shellmux = { path = "../shellmux" }
marsh-tui = { path = "../marsh-tui" }
# The REST/WebSocket surface `--rest-api` joins onto the console session; axum stays its dependency.
marsh-rest = { path = "../marsh-rest" }
brush-core.workspace = true
brush-builtins.workspace = true
brush-interactive = { path = "../brush-interactive", features = [
    "basic",
    "minimal",
    "reedline",
] }
clap = { version = "4.6.0", features = ["derive"] }
libc = "0.2.183"
thiserror = "2"
# `signal` for the SIGWINCH task that follows the terminal's size onto every job, and `net` for the
# `AsyncFd` that forwards real keystrokes into the foreground job's terminal.
tokio = { version = "1.52.3", features = [
    "rt",
    "rt-multi-thread",
    "sync",
    "signal",
    "net",
] }
